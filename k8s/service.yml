apiVersion: v1
kind: Service
metadata:
  name: facultades-backend-service
  namespace: default
spec:
  selector:
    app: facultades-backend-app
  ports:
    - protocol: TCP
      port: 80
      name: http
      targetPort: 4000
  type: ClusterIP
